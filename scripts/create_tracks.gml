var track_to_replace = argument0;

var is_right = (track_to_replace.id == right_track.id)

var new_track = instance_create(x, y, TireTracks_obj);
new_track.image_angle = direction;

if (is_right)
{
    new_track.x_offset = 
}
